.home
  display flex
  flex-direction column
  justify-content center
  align-items center
  height 100%
  min-height 100vh
  background-color $main-bg
  background-image linear-gradient($main-bg-default, $main-bg)
  h1
    font-size 55px
  h2
    font-size 45px
  .social-networks
    display flex
    margin-top 30px
    .social-network
      margin 0 10px
